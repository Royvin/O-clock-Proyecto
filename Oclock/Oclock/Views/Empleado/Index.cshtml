<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>O'Clock - Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container-custom {
            width: 1200px;
            max-width: 95%;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
        }

        /* HEADER */
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 25px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 32px;
            font-weight: bold;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .clock-icon {
            width: 40px;
            height: 40px;
            border: 3px solid white;
            border-radius: 50%;
            position: relative;
        }

            .clock-icon::before {
                content: '';
                position: absolute;
                width: 2px;
                height: 12px;
                background: white;
                top: 6px;
                left: 50%;
                transform: translateX(-50%);
            }

            .clock-icon::after {
                content: '';
                position: absolute;
                width: 2px;
                height: 8px;
                background: white;
                top: 12px;
                left: 50%;
                transform: translateX(-50%) rotate(90deg);
                transform-origin: top;
            }

        .user-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .avatar {
            width: 45px;
            height: 45px;
            background: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #667eea;
            font-weight: bold;
        }

        /* MENU GRID */
        .modules-section {
            padding: 50px;
            background: #fafbfc;
        }

        .module-card {
            background: white;
            border-radius: 18px;
            padding: 30px;
            text-align: center;
            box-shadow: 0 8px 20px rgba(0,0,0,0.08);
            transition: 0.25s;
            cursor: pointer;
            border: 1px solid #e2e8f0;
        }

            .module-card:hover {
                transform: translateY(-6px);
                box-shadow: 0 14px 30px rgba(0,0,0,0.15);
            }

        .module-icon {
            width: 65px;
            height: 65px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 15px auto;
            background: #eef0ff;
            color: #667eea;
            font-size: 30px;
        }

        .module-title {
            color: #2d3748;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .module-desc {
            color: #718096;
            font-size: 14px;
        }


    </style>
</head>
<body>

    <div class="container-custom">

        <!-- HEADER -->
        <div class="header">
            <div class="logo">
                <div class="clock-icon"></div>
                O'Clock
            </div>

            <div class="user-info">
                <div>
                    <div style="text-align: right; margin-bottom: 4px; font-weight: 600;">María González</div>
                    <div style="font-size: 13px; opacity: 0.9;">Colaboradora</div>
                </div>
                <div class="avatar">MG</div>
            </div>
        </div>

        <!-- MODULE MENU -->
        <div class="modules-section">

            <!-- Intro -->
            <div class="mb-5">
                <h2 style="font-weight:700; color:#2d3748;">Panel de Administración</h2>
                <p style="color:#718096; max-width:600px;">
                    Bienvenido a tu panel O’clock.
                </p>
            </div>

            <!-- Módulos principales -->
            <div class="row g-4 mb-5">

                <div class="col-md-4">
                    <a href="/Empleado/Marcas" class="menu-link">
                        <div class="module-card">
                            <div class="module-icon">⏰</div>
                            <div class="module-title">Marcas</div>
                        </div>
                    </a>
                </div>

                <div class="col-md-4">
                    <a href="/Empleado/HistorialMarcas" class="menu-link">
                        <div class="module-card">
                            <div class="module-icon">📝</div>
                            <div class="module-title">Mi Historial</div>
                        </div>
                    </a>
                </div>

                <div class="col-md-4">
                    <a href="/Empleado/Solicitudes" class="menu-link">
                        <div class="module-card">
                            <div class="module-icon">🗂️</div>
                            <div class="module-title">Mis Solicitudes</div>
                        </div>
                    </a>
                </div>

            </div>

            

        </div>

    </div>

</body>
</html>
